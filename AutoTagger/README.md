# Auto-tagger Transform

Automatically assign a set of tag key-value pairs to all supported resources in the stack.

## Usage

1) Execute deploy.sh and enter an existing S3 bucket name to store the SAM packaged template for deployment
2) Run the example.yaml stack and modify as required by filling in the Metadata["AddTags"] section
